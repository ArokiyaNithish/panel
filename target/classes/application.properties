spring.application.name=portal
server.port=8080
server.address=0.0.0.0
# Supabase PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://db.acoottddjqefzclhibit.supabase.co:5432/postgres?prepareThreshold=0
spring.datasource.username=postgres
spring.datasource.password=ntNWhl2fWqIN9bjz
spring.datasource.driver-class-name=org.postgresql.Driver



spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# 👉 For first setup only use 'update'. After tables are created → switch to 'validate'
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.show-sql=true

# Supabase requires SSL
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

# File Upload
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
file.upload-dir=uploads

# Flyway Database Migrations (For Production)
# 👉 Keep disabled for first run while using ddl-auto=update. Enable when ready.
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Gemini AI Settings (Update with your actual API key)
gemini.api.key=YOUR_GEMINI_API_KEY
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent

# Mail Configuration (Gmail App Password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vtu24347@veltech.edu.in
spring.mail.password=hmfc pomo zhmu ykfu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.com.campus=INFO
